# 🎓 Викторина по наукам

## 📑 Содержание

- [Описание](#-описание)
- [Особенности](#-особенности)
- [Скриншоты](#-скриншоты)
- [Быстрый старт](#-быстрый-старт)
  - [Требования](#требования)
  - [Установка](#установка)
- [Структура проекта](#-структура-проекта)
- [Использование](#-использование)
- [Технологии](#-технологии)
- [Архитектура кода](#-архитектура-кода)
- [Кастомизация](#-кастомизация)
- [Сборка](#-сборка-для-платформ)
- [Вклад в проект](#-вклад-в-проект)

---

## 📱 Описание

**Викторина по наукам** — это образовательное приложение-игра, разработанное на Flutter, которое позволяет пользователям проверить свои знания по трем основным предметам:

| Предмет | Описание | Количество вопросов |
|---------|----------|---------------------|
| 📐 **Математика** | Задачи на вычисления, алгебру и геометрию | 8 |
| ⚛️ **Физика** | Вопросы о законах физики, формулах и явлениях | 8 |
| 🧪 **Химия** | Вопросы о химических элементах, формулах и реакциях | 8 |

**Всего в базе:** 24 вопроса (случайный выбор 10 вопросов для каждой игры)

---

## ✨ Особенности

### 🎯 Игровой процесс
- ✅ **10 вопросов** в каждой игре (случайный выбор из базы)
- ⏱️ **Таймер** на каждый вопрос (30 секунд)
- 🎨 **Красивый UI** с градиентными фонами и анимациями
- 📊 **Система подсчета очков** с визуальной обратной связью
- 🏆 **Определение победы/поражения** (60% правильных ответов для победы)

### 📱 Платформы
- ✅ **Android** — все размеры экранов
- ✅ **iOS** — iPhone и iPad
- ✅ **Web** — Progressive Web App (PWA)

### 🎨 Дизайн
- Градиентные фоны для каждого предмета
- Анимации и переходы
- Адаптивный дизайн
- Material Design 3

---

## 📸 Скриншоты

> *Скриншоты будут добавлены позже*

---

## 🚀 Быстрый старт

### Требования

| Компонент | Версия | Описание |
|-----------|--------|----------|
| **Flutter SDK** | 3.9.2+ | Основной фреймворк |
| **Dart SDK** | 3.9.2+ | Язык программирования |
| **IDE** | - | Android Studio / VS Code с расширениями Flutter |
| **Xcode** | - | Только для iOS (macOS) |

### Установка

#### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd MST_test4
```

#### Шаг 2: Установка зависимостей

```bash
flutter pub get
```

#### Шаг 3: Запуск приложения

```bash
# Для всех доступных устройств
flutter run

# Для конкретной платформы
flutter run -d chrome        # Web
flutter run -d android       # Android
flutter run -d ios           # iOS (только macOS)
```

---

## 🏗️ Структура проекта

```
MST_test4/
├── lib/
│   ├── main.dart                    # Точка входа приложения
│   │
│   ├── models/                      # Модели данных
│   │   └── question.dart            # Модель вопроса
│   │
│   ├── data/                        # Данные и репозитории
│   │   └── questions.dart           # База вопросов (24 вопроса)
│   │
│   ├── screens/                     # Экраны приложения
│   │   ├── home_screen.dart        # Главное меню
│   │   ├── game_screen.dart        # Экран игры
│   │   └── result_screen.dart      # Экран результатов
│   │
│   ├── widgets/                     # Переиспользуемые виджеты
│   │   ├── subject_badge.dart      # Бейдж предмета
│   │   ├── timer_widget.dart      # Виджет таймера
│   │   ├── score_widget.dart       # Виджет счета
│   │   └── progress_bar.dart      # Прогресс-бар
│   │
│   ├── components/                  # UI компоненты
│   │   ├── answer_button.dart      # Кнопка ответа
│   │   ├── question_card.dart     # Карточка вопроса
│   │   ├── gradient_background.dart # Градиентный фон
│   │   └── primary_button.dart    # Основная кнопка
│   │
│   ├── utils/                       # Утилиты
│   │   └── subject_utils.dart      # Утилиты для работы с предметами
│   │
│   └── constants/                   # Константы
│       └── app_constants.dart     # Константы приложения
│
├── assets/
│   └── app_icon.png                 # Иконка приложения (1024x1024)
│
├── android/                          # Android конфигурация
├── ios/                              # iOS конфигурация
├── web/                              # Web конфигурация
│
├── pubspec.yaml                      # Зависимости проекта
└── README.md                         # Документация
```

---

## 🎮 Использование

### Как играть

1. **Запустите приложение** и нажмите кнопку **"Начать игру"**
2. **Читайте вопросы** внимательно
3. **Выбирайте правильный ответ** из предложенных вариантов
4. **Следите за таймером** — у вас есть 30 секунд на каждый вопрос
5. **Получайте обратную связь:**
   - ✅ Зеленый цвет = правильный ответ
   - ❌ Красный цвет = неправильный ответ
6. **Просматривайте результаты** после 10 вопросов
7. **Побеждайте!** — 60% правильных ответов = победа 🎉

### Правила игры

| Параметр | Значение |
|----------|----------|
| Количество вопросов | 10 |
| Время на вопрос | 30 секунд |
| Порог победы | 60% правильных ответов |
| Всего вопросов в базе | 24 |

---

## 🛠️ Технологии

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Flutter** | 3.9.2+ | Кроссплатформенный UI фреймворк |
| **Dart** | 3.9.2+ | Язык программирования |
| **Material Design** | 3 | Дизайн-система |

### Зависимости

```yaml
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1
```

---

## 📝 Архитектура кода

### Модели данных

| Класс | Описание | Файл |
|-------|----------|------|
| `Question` | Модель вопроса с вариантами ответов и категорией | `models/question.dart` |
| `Subject` | Enum для категорий (математика, физика, химия) | `models/question.dart` |

### Экраны (Screens)

| Экран | Описание | Файл |
|-------|----------|------|
| `HomeScreen` | Главное меню с информацией о предметах | `screens/home_screen.dart` |
| `GameScreen` | Основной игровой экран с вопросами и таймером | `screens/game_screen.dart` |
| `ResultScreen` | Экран результатов с подсчетом очков | `screens/result_screen.dart` |

### Компоненты (Components)

| Компонент | Описание | Файл |
|-----------|----------|------|
| `AnswerButton` | Интерактивная кнопка ответа с визуальной обратной связью | `components/answer_button.dart` |
| `QuestionCard` | Карточка с текстом вопроса | `components/question_card.dart` |
| `GradientBackground` | Переиспользуемый градиентный фон | `components/gradient_background.dart` |
| `PrimaryButton` | Стилизованная основная кнопка | `components/primary_button.dart` |

### Виджеты (Widgets)

| Виджет | Описание | Файл |
|--------|----------|------|
| `SubjectBadge` | Бейдж с иконкой и названием предмета | `widgets/subject_badge.dart` |
| `TimerWidget` | Отображение оставшегося времени | `widgets/timer_widget.dart` |
| `ScoreWidget` | Отображение текущего счета | `widgets/score_widget.dart` |
| `ProgressBar` | Прогресс-бар прохождения игры | `widgets/progress_bar.dart` |

### Утилиты (Utils)

| Утилита | Описание | Файл |
|---------|----------|------|
| `SubjectUtils` | Утилиты для работы с предметами (цвета, иконки, названия) | `utils/subject_utils.dart` |

### Константы (Constants)

| Константа | Значение | Описание |
|-----------|----------|----------|
| `totalQuestions` | 10 | Количество вопросов в игре |
| `questionTimeLimit` | 30 | Время на вопрос (секунды) |
| `winThreshold` | 0.6 | Порог победы (60%) |

---

## 🎨 Кастомизация

### Изменение количества вопросов

Откройте файл `lib/constants/app_constants.dart`:

```dart
class AppConstants {
  static const int totalQuestions = 10; // Измените на нужное значение
  // ...
}
```

### Изменение времени на вопрос

```dart
class AppConstants {
  static const int questionTimeLimit = 30; // Измените на нужное значение (в секундах)
  // ...
}
```

### Изменение порога победы

```dart
class AppConstants {
  static const double winThreshold = 0.6; // 60% для победы (0.0 - 1.0)
  // ...
}
```

### Добавление новых вопросов

Откройте файл `lib/data/questions.dart` и добавьте новые вопросы в метод `getAllQuestions()`:

```dart
Question(
  question: 'Ваш вопрос?',
  options: ['Вариант 1', 'Вариант 2', 'Вариант 3', 'Вариант 4'],
  correctAnswerIndex: 0, // Индекс правильного ответа (0-3)
  subject: Subject.mathematics, // или physics, chemistry
),
```

### Изменение цветов предметов

Откройте файл `lib/utils/subject_utils.dart` и измените цвета в методах `getSubjectColors()` и `getSubjectColor()`.

---

## 📦 Сборка для платформ

### Android

```bash
# APK файл
flutter build apk

# App Bundle (для Google Play)
flutter build appbundle

# Релизная версия
flutter build apk --release
```

### iOS

```bash
# Сборка для iOS
flutter build ios

# Для симулятора
flutter build ios --simulator
```

### Web

```bash
# Сборка для Web
flutter build web

# С оптимизацией
flutter build web --release
```

---

## 📱 Иконка приложения

Иконка приложения с вопросительным знаком настроена для всех платформ:

| Платформа | Статус | Размеры |
|-----------|--------|---------|
| **Android** | ✅ Готово | Все размеры (mdpi, hdpi, xhdpi, xxhdpi, xxxhdpi) |
| **iOS** | ✅ Готово | Все размеры для iPhone и iPad |
| **Web** | ✅ Готово | Favicon и PWA icons (192x192, 512x512) |

**Расположение:** `assets/app_icon.png` (1024x1024)

**Генерация иконок:**
```bash
flutter pub run flutter_launcher_icons
```
